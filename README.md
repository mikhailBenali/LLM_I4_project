# LLM_I4_project

First, create a conda environment with the env file : 

```bash
conda env create --name LLM_I4_project --file=environment.yml
```

Then activate it : 

```bash
conda activate LLM_I4_project
```

To run the streamlit interface run  :

```bash
streamlit run main.py
```
